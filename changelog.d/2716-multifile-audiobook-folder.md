### Fixed
- **Multi-file audiobooks on a library scan** (#2716) — a scanned book folder now records the folder as the audiobook, so every track in it moves or deletes with the book instead of only the first track that matched.
